import { IList, ListAction, ListContext, ListItem } from 'coc.nvim';
export default class Gitignore implements IList {
    private templatesPath;
    readonly name = "gitignore";
    readonly description = "gitignore templates";
    readonly defaultAction = "append";
    actions: ListAction[];
    private templateMap;
    private isInit;
    constructor(templatesPath: string);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
